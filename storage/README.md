# Storage Module

This directory contains the RTL sources for the storage subsystem of the TITAN-X SoC.

## File Descriptions

- **`mmc_controller.v`**: eMMC / SD Card host controller.
- **`qspi_controller.v`**: Quad-SPI flash memory controller.
- **`usb_otg.v`**: USB On-The-Go (OTG) controller.
