# Top Module

This directory contains the RTL sources for the top subsystem of the TITAN-X SoC.

## File Descriptions

- **`titan_x_top.v`**: Top-level SoC module integrating all subsystems (CPU, Interconnect, Peripherals, Memory).
